node_modules/
.next/
.env*.local
.env
.vercel
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

.vercel
.env*

# Review scratch: prompts, screenshots, verdicts, tsc snapshots. Working notes
# about the repo, never part of it. Untracked is not enough on its own, because
# one "git add -A" sweeps the whole directory in.
.hermes/
